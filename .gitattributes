# 统一按 LF 存储，检出时再按平台转换
* text=auto eol=lf

# Windows 批处理必须是 CRLF，否则 cmd.exe 可能解析异常
*.cmd text eol=crlf
*.bat text eol=crlf

# PowerShell
*.ps1 text eol=crlf

# 二进制
*.exe binary
*.dll binary
*.png binary
*.ico binary
