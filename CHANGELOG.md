# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.1.0] - 2026-09-24

### 新增

- **系统托盘常驻**
  - 点窗口关闭按钮不再退出，而是收进任务栏右下角的托盘图标，桥接继续工作
  - 鼠标悬停显示当前状态；左键单击打开主窗口；右键菜单可
    显示主界面 / 开始·停止桥接 / 退出
  - 启动时会有一次气泡提示，告诉用户程序收进托盘了
  - 「设置 → 行为」新增开关「点击关闭按钮时最小化到托盘」（默认开启），
    关掉它就恢复成点 ✕ 直接退出
  - `--minimized` 启动参数改为直接收进托盘，配合开机自启不会弹窗打扰
- **图标**：改用设计稿渲染出的 `assets/app.ico`（16~256 共 7 个尺寸），
  exe 与托盘使用同一份图标
  - 新增 `tools/make_icons.py`：优先读取位图设计稿 `assets/app-ICON.png`
    （内置纯标准库 PNG 解码器），没有位图时回退到矢量稿
    `assets/icon-source.eps`（内置迷你 PostScript 解释器）；
    全程不需要 Pillow / Ghostscript / ImageMagick
  - 托盘图标按系统真实小图标尺寸装载（125% 缩放取 20px 而非虚拟化的 16px），
    高 DPI 下不发虚
  - **标题栏 / 任务栏图标**：以前显示的是 Tk 窗口类自带的那根羽毛
    （Tk 的 logo），因为 Tk 建窗时并不设置 `WM_GETICON`，系统只能落到类图标上。
    现在通过 `iconbitmap` + `WM_SETICON` 换成自己的图标；
    注意 Tk 在窗口首次映射时会**重建顶层窗口**（句柄会变），
    所以图标必须在映射完成后再设置，并绑定 `<Map>` 以便从托盘恢复后重新设上

### 实现说明

- 托盘功能用 `Shell_NotifyIcon` + 运行时生成/装载 ICO 实现，
  **不引入 pystray / Pillow / pywin32**，继续保证运行时零第三方依赖
- 若 `assets/app.ico` 缺失，会自动退回用代码绘制的图形（并按状态换色）

## [1.0.0] - 2026-09-21

首个公开版本。

### 新增

- **图形界面**（`src/bridge_gui.py`，tkinter，无第三方依赖）
  - 主控页：开关位置指示灯、语音输入状态、实时麦克风电平条（带阈值刻度）、
    开始/停止、测一次电平、实时监测 10 秒、紧急松开按键
  - 设置页：快捷键、静音判定阈值、采集时长、拨动后等待、去抖、
    HID 设备选择、录音设备选择、开机自启等
  - 原理与说明页：信号链路图 + 完整原理说明
  - 日志页：实时事件日志
- **命令行版**（`src/g2_voice_bridge.py`），与界面版共用同一套核心逻辑
- **设备自适应**：自动枚举消费类(0x0C) HID 接口，并可在界面中选择录音设备，
  方便在换了耳机或换了电脑之后继续使用
- **单文件发布包**：`build_exe.cmd` 一键用 PyInstaller 打出免安装 exe
- **诊断工具**（`tools/`）：HID 报告抓取、麦克风电平表、Core Audio 静音监视、
  组合键注入测试等
- 打包版支持 `--list` / `--probe` / `--selftest` 三个自助排查入口

### 核心设计

- 开关位置**通过实测麦克风电平判断**，而不是靠统计拨动次数。
  耳机静音时会在硬件上切断话筒信号（实测峰值 ≈ 1，开启时 ≥ 200），
  因此拔插耳机、漏掉事件、一次拨动产生两个事件都不会导致开/关反向。

### 已知限制

- 依赖"耳机静音时切断话筒信号"这一硬件特性。不具备该特性的耳机无法使用
  （可用 `--probe` 或界面上的「测一次电平」快速验证）。
- 微信输入法 Windows 版是**长按说话**，因此语音输入期间程序会真的按住
  `Ctrl+Win`，这段时间内敲键盘会带上 Win 修饰键。
- 仅支持 Windows。
